# Run workspace

Training, evaluation, benchmark, and synthesis outputs remain ignored. Each
reproducible run should record the code revision, locked environment, dataset
manifest, configuration, seed, hardware profile, and resulting checksums.

