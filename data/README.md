# Data workspace

This directory is for local and generated dataset material. Raw, interim, and
processed audio remain ignored. Git may contain only reviewed schemas,
manifests, checksums, and tiny redistributable test fixtures.

Every accepted clip must reference explicit consent, provenance, a transcript,
language metadata, and a content hash. Do not collect recordings until the
governance gate is approved.

